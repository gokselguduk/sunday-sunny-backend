# Kök npm install: monoreposta postinstall backend’i kurar; yalnızca backend kökünde de çalışır.
[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm install --omit=dev"]

[start]
cmd = "npm start"
