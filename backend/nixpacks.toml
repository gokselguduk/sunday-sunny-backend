# Railway: Root Directory = "backend" iken bu klasör kök olur.
# Büyük sıçrama + 5g simülasyon: BIG_SPIKE_* ve BIG_SPIKE_SIM_* (opsiyonel) — dashboard’ta tanımlanır.
[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci --omit=dev || npm install --omit=dev"]

[start]
cmd = "npm start"
