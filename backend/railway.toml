# Sadece Railway'de Root Directory = "backend" ise geçerli.
# Öneri: Root'u repo kökü bırakıp kökteki Dockerfile ile deploy (daha tutarlı).
# https://docs.railway.com/guides/monorepo

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
