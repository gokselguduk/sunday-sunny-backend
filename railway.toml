# NIXPACKS. Start: npm start — monorepo kökünde ./backend’e yönlendirir;
# Railway Root Directory = backend iken de aynı package.json node src/index.js çalıştırır.
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
