# Root Directory = "." (repo kökü) — Nixpacks + kök nixpacks.toml
# Docker kullanmak istersen: builder = "DOCKERFILE" + dockerfilePath = "Dockerfile"
[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
